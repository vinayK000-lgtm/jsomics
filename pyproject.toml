[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "jsomics"
version = "1.0.0"
description = "JSOMICS — AI-powered multi-omics research intelligence platform"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.111.0",
  "uvicorn[standard]>=0.30.0",
  "pydantic>=2.7.0",
  "pydantic-settings>=2.2.0",
  "PyJWT>=2.9.0",
  "supabase>=2.5.0",
  "httpx>=0.27.0",
  "python-dotenv>=1.0.0",
  "python-multipart>=0.0.9",
  "psycopg[binary]>=3.2.0",
]

[project.optional-dependencies]
storage = [
  "psycopg[binary]>=3.2.0",
  "pgvector>=0.3.0",
]
models = [
  "torch>=2.3.0",
  "transformers>=4.42.0",
  "sentence-transformers>=3.0.0",
]
dev = [
  "pytest>=8.2.0",
  "pytest-asyncio>=0.23.0",
  "httpx>=0.27.0",
  "ruff>=0.5.0",
]

[tool.setuptools.packages.find]
include = ["bio_research_ai*", "jsomics_api*"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
